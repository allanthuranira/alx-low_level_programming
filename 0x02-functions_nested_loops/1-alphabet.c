#include "main.h"
/**
*print_alphabet - prints the alphabet in lowercase, followed by a new lin
*/
void print_alphabet(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
{
		_putchar(letter);
		_putchar('\n');
	
}
